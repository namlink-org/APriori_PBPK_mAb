# ── R session artefacts ────────────────────────────────────────────────────
.Rproj.user
.Rhistory
.RData
.Ruserdata
.positai
.claude/

# ── Excel temp / lock files ────────────────────────────────────────────────
~$*.xlsx

# ── Quarto cache (re-generated on render) ─────────────────────────────────
*_cache/

# ── Simulation result sub-folders (large CSVs, re-generated by Script 02) ─
# All intermediate runs are excluded; only the latest reference run is kept.
Test2/03_Apriori/V2/SimsOutputs/SimulationResults/*/
!Test2/03_Apriori/V2/SimsOutputs/SimulationResults/2026-06-17 19-50/

# ── Test1 — archived prototype, not the active codebase ───────────────────
Test1/

# ── Internal developer / AI notes (not part of the project) ───────────────
CLAUDE.md
KNOWLEDGE_TRANSFER.md